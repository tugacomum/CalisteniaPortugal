import React from 'react'
import { View,  StyleSheet, ImageBackground } from 'react-native';
import { Dimensions } from 'react-native';

export default function DescExercise() {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({
    img: {
        width: '100%',
        height: '100%'
    },
});